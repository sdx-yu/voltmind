# 笔不怠字体语义与页面层级优化验收报告 v2.3.2

验收日期：2026-08-31

源码提交：`cec2217`

结论：本机工程与 Apple Silicon 内测交付通过；公开分发仍为 NO-GO。

## 本次范围

- 工作台、操作标题、栏目标题、弹窗、空状态和数据指标统一使用界面无衬线体；
- 书名、章节名、场景名、人物名、正文和引文保留内容衬线体；
- 混合页面按“操作界面无衬线、创作内容衬线”建立稳定边界；
- `PageHeader` 默认使用工具字体，只有显式 `editorial` 语义才使用衬线体；
- 数据指标启用等宽数字，补充字体语义静态门和组件测试；
- 更新纸张、暗色和窄屏触控三套视觉基线；
- 产品版本、Tauri 版本、Rust 包版本和 PWA 缓存版本统一升级至 2.3.2。

## 验收结果

- UI 基础检查与 UI 质量检查：通过；
- TypeScript 类型检查：通过；
- Vitest：48 个文件、183 项测试全部通过；
- Vite 生产构建：2251 个模块构建通过；
- Playwright：10 项浏览器、无障碍、响应式与性能验收全部通过；
- 浏览器计算样式复核：工具标题为界面无衬线体，书名、场景名、人物名与正文为内容衬线体；
- macOS 应用结构签名：通过（ad-hoc）；
- App / 构建版本：均为 2.3.2；
- DMG 校验和 App ZIP 解压测试：通过；
- 真实启动：本地服务健康接口返回 `ok=true`、`integrity=ok`、`rescueMode=false`，首页标题与 slogan 正确；测试退出后应用、sidecar 与监听端口均已释放。

## 安装包

- `笔不怠_2.3.2_aarch64.dmg`：45,075,161 bytes；SHA-256 `3a97a3dbfefd1274552f5c8382ed778f28aeb213e243b096d76da27979e2c777`；
- `笔不怠_2.3.2_aarch64.app.zip`：45,102,876 bytes；SHA-256 `0dd88bcb01cb044ecfedabaf542b3d43a81dc21b2b8a08c276f8c093ab618b2d`。

## 发布边界

当前安装包适合本机或受控内测。它没有 Apple Developer ID 正式签名与 Apple 公证；Windows、移动端、真实种子作者和真实两周验证仍依赖外部条件，因此不能标记为公开上线版本。
