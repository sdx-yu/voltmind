# 笔不怠伏笔操作栏 UI 修复验收报告 v2.3.1

验收日期：2026-08-31

源码提交：`72976fd`

结论：本机工程与 Apple Silicon 内测交付通过；公开分发仍为 NO-GO。

## 本次范围

- “建立伏笔”恢复设计系统主按钮样式、紧凑高度和清晰文字对比度；
- 建立伏笔与阶段变更弹窗统一使用标准按钮组件；
- 操作栏增加明确的摘要/操作语义；
- 620px 以下改为纵向操作栏，按钮占满可用宽度，只有四列看板横向滚动，不再撑宽整页；
- 产品版本、Tauri 版本和 PWA 缓存版本统一升级至 2.3.1。

## 验收结果

- UI 基础检查与 UI 质量检查：通过；
- TypeScript 类型检查：通过；
- Vitest：48 个文件、182 项测试全部通过；
- Vite 生产构建：2251 个模块构建通过；
- Playwright：10 项浏览器、无障碍、响应式与性能验收全部通过；
- 人工浏览器复核：桌面按钮为 32px 高；600px 窄屏页面无横向溢出，看板可独立横向滚动；
- macOS 应用结构签名：通过（ad-hoc）；
- App / 构建版本：均为 2.3.1；
- DMG 校验、App ZIP 解压测试与 5 项发布物料 SHA-256：通过；
- 真实启动：本地服务健康接口返回 `ok=true`、`integrity=ok`、`rescueMode=false`，首页标题与 slogan 正确；测试退出后应用、sidecar 与监听端口均已释放。

## 安装包

- `笔不怠_2.3.1_aarch64.dmg`：45,074,547 bytes；SHA-256 `e1f6699d52b7cab1c7d6272f970fd8ff8fd58fcae1abdaf3c110b2b7f7d23471`；
- `笔不怠_2.3.1_aarch64.app.zip`：45,101,495 bytes；SHA-256 `ab3b635d9a789e511374470c4e48df04df3c2e8e588685859a84ec061705183c`。

## 发布边界

当前安装包适合本机或受控内测。它没有 Apple Developer ID 正式签名与 Apple 公证；Windows、移动端、真实种子作者和真实两周验证仍依赖外部条件，因此不能标记为公开上线版本。
