# 笔不怠作品信息编辑闭环验收报告 v2.3.3

验收日期：2026-08-31

源码提交：`cc89d77`

结论：本机工程与 Apple Silicon 内测交付通过；公开分发仍为 NO-GO。

## 本次范围

- 书架作品卡片增加“更多操作 → 编辑作品信息”；
- 工作区左上角书名变为可访问的编辑入口；
- 两处共用书名与作品简介编辑弹窗；
- 保存后即时更新书架和工作区项目状态；
- 导出文件名、投稿书名页与备份继续读取同一个项目名称；
- 数据库在同一事务内同步项目名称与书稿根节点名称；
- 弹窗打开后聚焦并选中书名，关闭后将焦点返回入口；
- 触控设备始终显示作品卡片操作入口；
- 产品版本、Tauri 版本、Rust 包版本和 PWA 缓存版本统一升级至 2.3.3。

## 验收结果

- UI 基础检查与 UI 质量检查：通过；
- TypeScript 类型检查：通过；
- Vitest：48 个文件、187 项测试全部通过；
- Vite 生产构建：2252 个模块构建通过；
- Playwright：10 项浏览器、无障碍、响应式与性能验收全部通过；
- 真实浏览器交互：书架和工作区入口、保存后即时刷新、弹窗初始焦点与焦点恢复通过；
- 数据库检查：项目名称与书稿根名称保持一致；
- macOS 应用结构签名：通过（ad-hoc）；
- App / 构建版本：均为 2.3.3；
- DMG 校验和 App ZIP 解压测试：通过；
- 真实启动：本地服务健康接口返回 `ok=true`、`integrity=ok`、`rescueMode=false`，首页标题与 slogan 正确；测试退出后应用、sidecar 与监听端口均已释放。

## 安装包

- `笔不怠_2.3.3_aarch64.dmg`：45,080,661 bytes；SHA-256 `b6c03c29f880e33ff73d2e9804ddfb53b94a9c1e6443a9366b6389a40f725951`；
- `笔不怠_2.3.3_aarch64.app.zip`：45,108,627 bytes；SHA-256 `c868bf805ac58aa894956d14b466e79012722fe6d12524119bf9c90b3f98f853`。

## 发布边界

当前安装包适合本机或受控内测。它没有 Apple Developer ID 正式签名与 Apple 公证；Windows、移动端、真实种子作者和真实两周验证仍依赖外部条件，因此不能标记为公开上线版本。
