# 笔不怠 R1-C 受控两周波次执行验收报告 v1.6.0

验收日期：2026-08-27（Asia/Singapore）

基线提交：`254e0fa`（R1-B）

结论：**R1-C 波次执行能力本机工程通过；真实外部波次未开始，R1 公开发布 NO-GO。**

## 1. 本阶段交付

- schema v16：`research_waves`、不可变 `research_wave_events`、有界 `research_wave_incidents`，参与者增加不可换的 `wave_id`；v15 升级前自动完整备份；
- `r1c-wave-v1`：真实受控/工程演练类型，14–42 天 UTC 窗口，真实目标 5–10 人，四类固定配额总和与目标一致；
- 四项准备门：协议审阅、产品外访问受控名册、删除联系人、支持/恢复演练；
- 状态机：草稿→招募中→执行中↔暂停→复核→关闭，并支持受控取消；真实波次只能在冻结窗口执行，窗口结束后复核；
- 研究包归属同时校验证据类型、同意窗口、总目标和分层配额；首次归属后不可换波次，失败的增量包不会留下半完成绑定；
- 固定异常码/级别、严重异常关闭阻断、顺序号与前向 SHA-256 事件链；无异常自由文本；
- 归属事件只保存波次域二次哈希承诺，不复制参与者哈希；
- `bbd-wave-kit-v1` 无名册执行包与 R1-C 执行台；工具不保存姓名/联系方式，不发送邀请，不自动改变 R1。

## 2. 自动化证据

执行 `npm run check`：

- TypeScript 应用端与服务端类型检查通过；
- Vitest **36 个文件、132 项测试全部通过**；
- R1-C 专项覆盖 v15→v16 备份迁移、窗口/目标/配额/准备门、外部执行时间、状态跳转、严重异常关闭阻断、事件哈希复算、无名册导出、HTTP 会话隔离、演练不计数；
- R1-B 联动覆盖类型匹配、真实/夹具隔离、分层配额、不可换波次、失败晚绑定原子回滚和关联承诺不复制参与者哈希；
- Vite 生产构建转换 **2,155 个模块**并成功产出；
- `npm audit --omit=dev`：**0 个漏洞**。

## 3. 真实浏览器验收

使用独立临时数据目录完成书架→R1 真实验证→研究负责人工作台→R1-C 波次执行台：

- 初始真实波次、真实参与者、两周达标均为 0，R1-C 显示 NO-GO 和“真实执行未开始”；
- 默认工程演练，四项准备未全选时创建禁用；全选后创建 1 人/14 天冻结演练草稿；
- 演练依次进入招募中和执行中，记录 `data_loss/critical` 后未解决异常为 1，解决后回到 0；
- 全流程真实波次/真实参与者/两周达标仍为 0，未把演练当作真实执行；
- 390×844 视口下 `innerWidth = document.scrollWidth = 390`，所有可见按钮未越界，NO-GO 可见；
- 浏览器 console warning/error 为 0。

## 4. 桌面与发行证据

- package、Tauri、Cargo、Service Worker 均为 1.6.0；
- Apple Silicon `.app`、DMG 与 Node SEA sidecar 构建通过；
- `codesign --verify --deep --strict` 通过；sidecar 保留 `allow-jit` 与 `allow-unsigned-executable-memory` entitlement；
- `hdiutil verify` 通过；发行检查确认当前为 ad-hoc 签名，非 Developer ID、未公证，Windows 签名安装包不存在，因此公开发布 NO-GO；
- 桌面应用启动后本阶段 sidecar 只监听随机 `127.0.0.1:60614`，`/api/health` 返回 `ok=true`、`integrity=ok`、`rescueMode=false`；启动按策略创建 SQLite 快照；
- SBOM 与生产许可证清单重新生成；
- `.app.zip`：44,629,222 bytes，SHA-256 `329c76c97b3edcb3007d751b817b5ec0bf19973d161c920bcfef4e66da714707`；
- `.dmg`：44,599,275 bytes，SHA-256 `99fcd4aefe69613bf89e050a80ad412e16848ecd2a57b9e5456095d39dcccea0`。

## 5. 未通过与下一步

当前真实外部波次为 **0**、真实参与者为 **0**、两周达标为 **0**。没有声称真实招募、身份、时间流逝、留存、因果效果、付费意愿或研究结论。浏览器与自动化只运行工程演练。

macOS 包仍为 ad-hoc 签名且未 Apple 公证；没有 Windows x64 签名安装包、真实中文输入法矩阵、移动真机与公开分发证据。因此 R1 公开发布继续 **NO-GO**。

下一步必须由研究负责人在产品外建立受控名册与安全交付渠道，创建首轮 `external_controlled` 波次，按真实招募和同意执行并等待冻结窗口结束。工程团队可修复真实执行中发现的问题，但不得用夹具、改时钟、补录或截图补足人数与周期。
