# 笔不怠故事蓝图与开篇引导验收报告 v2.9.0

## 验收结论

STORY-A 已通过。产品已形成“开篇选择规划方式 → 建立全书蓝图 → 维护关键节拍 → 关联正文场景 → AI 显式读取约束 → 备份／接力恢复”的完整闭环。故事蓝图与正文、模板彼此独立，不会因为采用规划功能自动生成或改写正文目录。

## 已交付

- 新建故事增加“先定故事方向”路径，可填写类型、一句话前提和结局落点，并原子化创建九个三幕关键节拍。
- “规划”增加故事蓝图视图，覆盖核心冲突、目标／需要、失败代价、主题、高潮抉择、结局状态、结局揭示、目标字数和创作边界。
- 节拍支持结构位置、剧情任务、预期变化、避坑提示、进度及多场景关联；删除节拍不删除正文。
- 删除场景时节拍链接隐藏，恢复场景后自动重现，底层关联不丢失。
- AI 上下文显式显示故事蓝图与当前场景节拍；仅作者已保存的信息可以进入上下文，AI 不回写蓝图。
- 普通备份、恢复、加密接力和旧项目迁移均覆盖故事蓝图；数据库升级到 schema v21。
- 桌面／iOS 工程与前端缓存版本统一为 2.9.0。

## 自动验收证据

- UI 基础门：通过；24 个组件槽、46 个语义令牌、3 套主题、3 套密度、无原生选择框和裸必填字段。
- UI 质量门：通过；最小字号 12px，10 组主题色对比全部达标。
- TypeScript 双端类型检查：通过。
- Vitest：56 个文件、245 项测试全部通过。
- Vite 生产构建：通过；2263 个模块完成转换。
- Playwright：14 项端到端测试全部通过，含新建引导、蓝图、九节拍、WCAG 严重问题扫描、窄屏回流和 20 万字稿性能门。
- 代码差异检查：`git diff --check` 通过。

## 验收中发现并修复

- 旧迁移测试仍以 schema v20 为目标，已统一升级到 v21，并验证迁移前快照命名与新表重建。
- 蓝图空字段提示和“尚未关联场景”的文字对比度不足，已提升到正文级可读色并通过 axe 扫描。
- 新端到端用例增加服务就绪重试，避免把本地服务启动窗口误判为功能失败。

## 明确未做

- 不提供自动编剧或一键生成整书大纲。
- 不强制作者采用三幕结构，九个默认节拍均可编辑或移除。
- 不在本阶段加入多情节线／人物弧泳道、场景任务卡和自动偏航判定；这些作为 STORY-B 候选，需结合真实使用反馈排优先级。

## 2.9.0 桌面交付

- 功能源码提交：`fd62c8b8449022ceeedb704fbd09f8db01ff15bc`；Windows 跨平台测试稳定性提交：`aee02324398323da0b2ec0089b9b6e63ccb9f2c8`。
- `笔不怠_2.9.0_aarch64.dmg`：50,098,202 bytes；SHA-256 `cd614954c6bd6254e9393805256cc8918cb466adcf25695d2da6ca2e996678d8`。
- `笔不怠_2.9.0_aarch64.app.zip`：45,421,046 bytes；SHA-256 `2fb64f8bfed8b369af5a3ac9410832e7d02beabdcee6b5e5bd3afaaefc318956`。
- 包内短版本与构建版本均为 2.9.0；`.app` 深度严格签名结构、DMG CRC、ZIP 完整性全部通过。
- 实际启动打包后的应用，本地 sidecar 健康接口返回 HTTP 200、数据库 integrity `ok`、rescueMode `false`。
- macOS 包仍为 ad-hoc 签名内测包；正式公开分发仍需 Developer ID 签名与 Apple 公证。

## 2.9.0 Windows x64 交付

- GitHub Actions [Windows x64 desktop package #13](https://github.com/sdx-yu/voltmind/actions/runs/33831766902) 全量通过，源提交为 `aee02324398323da0b2ec0089b9b6e63ccb9f2c8`。
- `笔不怠_2.9.0_x64-setup.exe`：29,054,656 bytes；SHA-256 `d09196cdf88c407cfaa2a354404788452419a5c932d91aeddfea9003e03726f7`。
- `笔不怠_2.9.0_x64_zh-CN.msi`：41,771,008 bytes；SHA-256 `77aae28dd5d0fa622126a5f080ea0a9b980bf42c7a6b08a96f9a42fe4333cf5e`。
- 云端构建证据状态为 `PASSED`；MSI 静默安装与卸载退出码均为 0；下载后的两个安装包再次通过 `SHA256SUMS` 校验。
- Windows 包为未签名内部测试包，可能触发 SmartScreen“未知发布者”；正式公开分发仍需 Authenticode 证书。
- macOS 与 Windows 共四个 2.9.0 安装包已放入本机“下载”；原四个 2.8.2 包已移入系统废纸篓，可恢复。
