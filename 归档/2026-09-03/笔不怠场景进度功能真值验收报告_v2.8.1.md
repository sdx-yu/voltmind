# 笔不怠场景进度功能真值验收报告 v2.8.1

## 结论

2.8.1 已完成场景进度工作流、场景状态卡和表单对齐规范的实现。普通进度不再混入“已完成／已发布”，完成状态只能由真实完成动作产生；终态正文发生实际修改后会自动回到“修订中”。

## 本次范围

- 将想法、计划、草稿、修订中定义为可直接切换的写作过程状态；
- 将已完成改为“完成本场景并提取事实”动作的结果；
- 保留已发布作为历史数据兼容状态，不伪造尚不存在的发布功能；
- 终态正文变更时自动进入修订，重复保存相同内容不改变状态；
- 同步和备份导入保留原有终态；
- 编辑器保存接口返回最新节点，使书稿树、标题区和右侧检查器即时同步状态；
- 重排场景状态卡，并统一多列表单的顶对齐、帮助文案和可选标识布局。

## 自动化覆盖

- 数据库：终态相同内容保持、不同内容进入修订；
- API：禁止通用接口伪造终态、完成动作、重复完成冲突、保存响应状态；
- 同步／备份：恢复后仍保留已完成；
- 组件：普通状态选项、完成动作、终态展示和进入修订；
- 真实浏览器：完成场景后修改正文，界面自动恢复“修订中”；
- 视觉回归：编辑器纸张视图、场景状态卡和多列表单对齐。

## 工程验收

- UI 基础门通过：24 个基础组件槽位、46 个语义色彩变量、3 套主题、3 档密度，原生选择器和裸必填字段均为 0；
- UI 质量与三主题 10 组关键对比度门通过，最小字号 12px；
- TypeScript 客户端与服务端类型检查通过；
- Vitest：55 个测试文件、230 项单元／集成测试全部通过；
- Vite 生产构建通过，共转换 2262 个模块；
- Playwright：13 项真实浏览器验收全部通过，包含状态工作流、表单对齐、视觉回归、WCAG、键盘、中文输入法、200% 回流、触控、响应式和二十万字性能门；
- 性能门修正了测试计时起点：先确认书架项目按钮可见，再测量打开大稿，8 秒阈值未放宽；
- 功能源码提交为 `99d19e7`；macOS 桌面包从该提交构建。

## 2.8.1 桌面交付

- `笔不怠_2.8.1_aarch64.dmg`：45,329,031 bytes；SHA-256 `8d0345eff78762514f6fdd703fca35dac15051d2e30d3d895ddc387ceefa2a7e`；
- `笔不怠_2.8.1_aarch64.app.zip`：45,358,275 bytes；SHA-256 `76455c59d1efa5076208b8aeee369a79f20004599ee016622e11a10e15110b09`；
- `.app` 深度严格代码签名结构、DMG CRC 完整性、ZIP 解压测试和包内 2.8.1 双版本号均通过；
- 两个 macOS 包已复制到用户“下载”目录，副本哈希与项目发布目录完全一致；
- 2.8.0 的项目发布物和“下载”目录副本已移入系统废纸篓，可恢复；项目发布目录只保留 2.8.1；
- Windows 2.8.1 已由 `v2.8.1` 标签触发 [GitHub Actions #10](https://github.com/sdx-yu/voltmind/actions/runs/33734664056)，版本校验、质量门、NSIS／简体中文 MSI 构建、哈希检查、MSI 静默安装与卸载均通过；
- `笔不怠_2.8.1_x64-setup.exe`：29,009,043 bytes；SHA-256 `8a277f084e38165df94383e68c28bd02e7f51695bf33f65b3ebe5729499deb5f`；
- `笔不怠_2.8.1_x64_zh-CN.msi`：41,705,472 bytes；SHA-256 `d183a941af79ff846aeaa19b4c7d99375fb750fcb24d00c30aadfb5592eb1bd5`；
- Windows 构建证据状态为 `PASSED`，MSI 安装与卸载退出码均为 0；两个包未做 Authenticode 签名，只可作为已知测试者内测包；
- “下载”目录最终只保留四个 2.8.1 安装文件：macOS DMG／App ZIP 与 Windows EXE／MSI；2.7.1 Windows 旧包已移入系统废纸篓。

## 产品边界

- “已发布”目前仅兼容历史数据；没有真实发布渠道、发布记录或撤回流程，因此不提供新增入口。
- 完成场景会提取事实并运行连续性检查，但不会伪称生成独立快照。
- 本次发布仍是内部测试交付；macOS 正式签名、公证及 Windows 正式代码签名仍需外部证书条件。
