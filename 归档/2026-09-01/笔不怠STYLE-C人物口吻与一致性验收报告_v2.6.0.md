# STYLE-C 人物口吻与一致性验收报告 v2.6.0

结论：通过。

完成范围：人物可单独设置语域、句长、表达直接度、情绪外露、说话习惯与禁忌；人物在当前正文被名称或别名提及时，其口吻契约进入 AI 上下文。检查页新增可解释的场景文风一致性分数与偏离证据。采用、丢弃和撤销按主风格与任务类型形成本机偏好统计。

真值规则：人物口吻只约束对白，不覆盖人物正典；一致性检查只基于可测特征，不把审美判断伪装成错误；偏好只保存计数，不保存候选正文，也不会自动静默修改文风，作者可一键清空。

证据：人物口吻 API/上下文注入、场景一致性、偏好记录、检查器交互及备份恢复测试通过；schema v20 完成。最终联合验收为 53 个测试文件、215 项测试全通过；UI 基础与质量门禁通过；生产构建通过；Playwright 10 项桌面、主题、键盘、响应式、WCAG 与长文性能场景全通过。

桌面交付：

- `笔不怠_2.6.0_aarch64.dmg`：45,281,809 bytes；SHA-256 `e41662d5771bf0c261a7df59cf392428be8406ca62c67c75568e63df0b5cf197`；
- `笔不怠_2.6.0_aarch64.app.zip`：45,307,536 bytes；SHA-256 `21954013a35f50cf05a8b298486dd7f433efec8135ada4e7e1ccd6266371b0ff`；
- `.app` 严格代码签名校验、DMG 完整性校验、ZIP 解压校验和全目录 SHA-256 复核通过；
- 当前为 Apple Silicon、ad-hoc 签名的 macOS 内测包，未冒充 Developer ID 签名或 Apple 公证；Windows 安装器必须在 Windows x64 签名环境构建，本次未生成。

未冒充通过：跨章节角色声线的真实辨识度与作者满意度仍需外部作者样本验证。
